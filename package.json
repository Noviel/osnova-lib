{
  "author": "Sasha snov Noviel",
  "dependencies": {},
  "description": "Authentication routine for OSNOVA",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.2",
    "babel-preset-latest": "^6.16.0"
  },
  "license": "ISC",
  "main": "build/index.js",
  "maintainers": [
    {
      "name": "sonsnov",
      "email": "snov.contact@gmail.com"
    }
  ],
  "name": "osnova-lib",
  "optionalDependencies": {},
  "scripts": {
    "build": "babel src -d build",
    "major-release": "npm --no-git-tag-version version major && npm publish",
    "minor-release": "npm --no-git-tag-version version minor && npm publish",
    "patch-release": "npm --no-git-tag-version version patch && npm publish",
    "test": "npm run build && node test/index.js"
  },
  "version": "0.6.1"
}
